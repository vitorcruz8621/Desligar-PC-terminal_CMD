O codigo javascript em 'index.html', ao ser executado, irá renderizar uma página em branco com um botão
CLICK.

Se o CLICK for precionado antes das 18:00, aparecerá duas mensagens:

1) comando para desligar o computador às 12:00;
2) comando para desligar o computador às 18:00;
